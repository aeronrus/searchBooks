import React, { useRef } from 'react';
import Headerx from '../components/Header/Headerx';

const Home: React.FC = () => {
  return (
    <div>
      <Headerx />
    </div>
  );
};
export default Home;
